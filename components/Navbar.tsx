import React from 'react';
import { Bell, School, Shield } from 'lucide-react';
import { MADRASAH_INFO } from '../data/madrasahData';
import { WeatherDashboard } from '../WeatherDashboard';

interface Props { activeTab: 'student' | 'admin'; setActiveTab: (tab: 'student' | 'admin') => void; onOpenAdminAuth: () => void; isAdminAuthenticated: boolean; isInsideGeofence: boolean; gpsDistance: number; }
export const Navbar: React.FC<Props> = ({ activeTab, setActiveTab, onOpenAdminAuth, isInsideGeofence, gpsDistance }) => <><header className="no-print sticky top-0 z-40 border-b border-slate-200 bg-white/95 shadow-sm backdrop-blur-md"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-3 sm:px-6"><div className="flex min-w-0 items-center gap-3"><div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl bg-emerald-600 text-white"><School className="h-6 w-6" /></div><div><h1 className="truncate text-base font-extrabold text-slate-900 sm:text-xl">{MADRASAH_INFO.name}</h1><p className="flex items-center gap-2 text-sm text-slate-500"><span className={`status-dot ${isInsideGeofence ? 'bg-emerald-500' : 'bg-rose-500'}`} />{isInsideGeofence ? 'Area Madrasah' : 'Luar Radius'} · {Math.round(gpsDistance)} m</p></div></div><nav className="flex items-center gap-2 rounded-2xl border border-slate-200 bg-slate-50 p-1"><button onClick={() => setActiveTab('student')} className={`rounded-xl px-4 py-2 text-sm font-bold ${activeTab === 'student' ? 'bg-emerald-600 text-white' : 'text-slate-600'}`}>Presensi Siswa</button><button onClick={onOpenAdminAuth} className={`flex items-center gap-2 rounded-xl px-4 py-2 text-sm font-bold ${activeTab === 'admin' ? 'bg-emerald-600 text-white' : 'text-slate-600'}`}><Shield className="h-4 w-4" />Guru</button><button className="rounded-xl p-2 text-slate-600" title="Notifikasi" aria-label="Notifikasi"><Bell className="h-5 w-5" /></button></nav></div></header><WeatherDashboard /></>;
