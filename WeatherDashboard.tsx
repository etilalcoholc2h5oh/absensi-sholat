import React, { useCallback, useEffect, useState } from 'react';
import { Cloud, Droplets, LocateFixed, Search, Wind, X } from 'lucide-react';

type WeatherLocation = { name: string; latitude: number; longitude: number; country?: string; admin1?: string };
type WeatherData = { current: { temperature_2m: number; relative_humidity_2m: number; apparent_temperature: number; weather_code: number; wind_speed_10m: number; precipitation: number }; daily: { time: string[]; weather_code: number[]; temperature_2m_max: number[]; temperature_2m_min: number[]; precipitation_probability_max: number[] }; timezone: string };
const DEFAULT_LOCATION: WeatherLocation = { name: 'MAN 1 Boyolali', latitude: -7.536065, longitude: 110.596245, country: 'Indonesia' };
function weatherLabel(code: number) { if (code === 0) return 'Cerah'; if ([1, 2, 3].includes(code)) return 'Berawan'; if ([45, 48].includes(code)) return 'Berkabut'; if ([51, 53, 55, 56, 57].includes(code)) return 'Gerimis'; if ([61, 63, 65, 66, 67, 80, 81, 82].includes(code)) return 'Hujan'; if ([95, 96, 99].includes(code)) return 'Badai petir'; return 'Kondisi tidak diketahui'; }
async function fetchWeather(location: WeatherLocation): Promise<WeatherData> { const params = new URLSearchParams({ latitude: String(location.latitude), longitude: String(location.longitude), current: 'temperature_2m,relative_humidity_2m,apparent_temperature,weather_code,wind_speed_10m,precipitation', daily: 'weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max', forecast_days: '7', timezone: 'auto' }); const response = await fetch(`https://api.open-meteo.com/v1/forecast?${params}`); if (!response.ok) throw new Error('Data cuaca tidak dapat dimuat.'); return response.json(); }
export const WeatherDashboard: React.FC = () => { const [location] = useState(DEFAULT_LOCATION); const [weather, setWeather] = useState<WeatherData | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [isOpen, setIsOpen] = useState(true); const load = useCallback(async () => { try { setWeather(await fetchWeather(location)); } catch (err) { setError(err instanceof Error ? err.message : 'Data cuaca gagal dimuat.'); } finally { setLoading(false); } }, [location]); useEffect(() => { void load(); }, [load]); return <section className="weather-shell max-w-7xl mx-auto mt-5" aria-label="Dashboard cuaca"><div className="flex justify-between mb-3"><div><p className="text-sm font-bold text-emerald-700 uppercase">Informasi cuaca</p><h2 className="text-2xl font-extrabold">Cuaca MAN 1 Boyolali</h2><p className="text-sm text-slate-500">Lokasi madrasah</p></div><button type="button" onClick={() => setIsOpen((v) => !v)} className="p-2 rounded-xl border border-slate-200" aria-label="Tampilkan atau sembunyikan cuaca">{isOpen ? <X /> : <Cloud />}</button></div>{isOpen && (loading ? <div className="weather-card p-6">Memuat data cuaca terbaru...</div> : error ? <div className="weather-card p-6 text-rose-700">{error}</div> : weather && <div className="weather-card p-5 sm:p-6"><div className="flex flex-col lg:flex-row justify-between gap-5"><div><p className="text-slate-500 font-semibold">{location.name}</p><p className="text-xs text-slate-500">Koordinat: {location.latitude.toFixed(6)}, {location.longitude.toFixed(6)}</p><div className="flex items-end gap-3"><span className="text-6xl font-black">{Math.round(weather.current.temperature_2m)}°</span><div><p className="text-xl font-bold">{weatherLabel(weather.current.weather_code)}</p><p className="text-slate-500">Terasa {Math.round(weather.current.apparent_temperature)}°</p></div></div></div><div className="grid grid-cols-2 sm:grid-cols-3 gap-3"><div className="weather-metric"><Droplets /><span>Kelembapan</span><strong>{weather.current.relative_humidity_2m}%</strong></div><div className="weather-metric"><Wind /><span>Angin</span><strong>{Math.round(weather.current.wind_speed_10m)} km/j</strong></div><div className="weather-metric"><Cloud /><span>Hujan</span><strong>{weather.current.precipitation} mm</strong></div></div></div><div className="border-t mt-6 pt-5"><h3 className="font-extrabold text-lg mb-3">Prakiraan 7 hari</h3><div className="grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-7 gap-2">{weather.daily.time.map((date, index) => <div key={date} className="weather-day"><span className="font-bold">{index === 0 ? 'Hari ini' : new Intl.DateTimeFormat('id-ID', { weekday: 'short', day: 'numeric' }).format(new Date(`${date}T12:00:00`))}</span><span className="text-2xl my-2">{weatherLabel(weather.daily.weather_code[index])}</span><strong>{Math.round(weather.daily.temperature_2m_max[index])}° / {Math.round(weather.daily.temperature_2m_min[index])}°</strong><small>Peluang hujan {weather.daily.precipitation_probability_max[index]}%</small></div>)}</div></div><p className="text-xs text-slate-500 mt-4">Sumber data: Open-Meteo</p></div>)}</section>; };
